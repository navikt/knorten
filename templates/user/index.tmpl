{{ define "user/index.tmpl" }}
    {{ template "head" }}
    <body>
    <div class="pure-g">
        <div class="pure-u-1-3"></div>
        <div class="pure-u-1-3">
            <h1>
                Så mange apper du har, sier Knorten
            </h1>
            {{ range .charts }}
                <h2>
                    {{ .Namespace }} (<a href="team/{{ .Namespace }}/edit">rediger</a>)
                </h2>
                <table class="pure-table">
                    <thead>
                    <tr>
                        <th>App</th>
                        <th>Adresse</th>
                        <th>Secret</th>
                        <th>Service Account</th>
                        <th>Rediger</th>
                    </tr>
                    </thead>
                    <tbody>
                    {{ if ne .App "namespace" }}
                        <tr>
                            <td>{{ .App }}</td>
                            <td><a href="{{ .Ingress }}">{{ .Ingress }}</a></td>
                            <td><a href="{{ .Secret }}">Secret Manager</a></td>
                            <td>{{ .ServiceAccount }}</td>
                            <td><a href="chart/{{ .App }}/{{ .Namespace }}/edit">Rediger</a></td>
                        </tr>
                    {{ end }}
                    </tbody>
                </table>
            {{ end }}
            <a class="pure-button pure-button-primary" href="/team/new">Nytt team</a>
            <a class="pure-button pure-button-primary" href="/chart/jupyterhub/new">Ny Jupyter</a>
            <a class="pure-button pure-button-primary" href="/chart/airflow/new">Ny Airflow</a>
        </div>
    </div>
    </body>
    </html>
{{ end }}